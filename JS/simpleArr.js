function simpleArr(ar) {
    let num = 0

    for(let i = 0; i < ar.length; i++){
        num += ar[i]
    }

    return num
}

// Given an array of integers, find the sum of its elements.

// For example, if the array , , so return .